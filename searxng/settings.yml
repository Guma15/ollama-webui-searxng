use_default_settings: true
server:
  bind_address: "0.0.0.0"
  secret_key: "${SEARXNG_SECRET_KEY}"  # Required in many setups; generate one e.g. openssl rand -hex 32
  limiter: false  # Optional, but explicit
search:
  formats:
    - html
    - json